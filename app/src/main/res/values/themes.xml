<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AnSu" parent="Theme.Material3.DynamicColors.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="actionBarTheme">@style/Base.Theme.AnSu.ActionBar</item>

        <item name="sudokuBorderColor">@color/material_dynamic_primary30</item>
    </style>

    <style name="Base.Theme.AnSu.ActionBar" parent="ThemeOverlay.MaterialComponents.ActionBar">
        <!-- Change overflow icon color -->
        <item name="colorControlNormal">@color/material_dynamic_primary40</item>
        <!-- Change drawable icon color -->
        <item name="iconTint">@color/material_dynamic_primary40</item>
        <!-- Change title text color -->
        <item name="titleTextColor">@color/material_dynamic_primary40</item>
    </style>

    <style name="Theme.AnSu" parent="Base.Theme.AnSu" />
</resources>