<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AnSu" parent="Theme.Material3.DynamicColors.DayNight">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimaryDark">@android:color/system_neutral1_900</item>

        <item name="actionBarTheme">@style/Base.Theme.AnSu.ActionBar</item>

        <item name="sudokuBorderColor">@android:color/system_accent1_600</item>
    </style>

    <style name="Base.Theme.AnSu.ActionBar" parent="ThemeOverlay.MaterialComponents.ActionBar">
        <!-- Change overflow icon color -->
        <item name="colorControlNormal">@android:color/system_accent1_100</item>
        <!-- Change drawable icon color -->
        <item name="iconTint">@android:color/system_accent1_100</item>
        <!-- Change title text color -->
        <item name="titleTextColor">@android:color/system_accent1_100</item>
    </style>
</resources>